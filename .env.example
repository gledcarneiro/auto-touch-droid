# ============================================================================
# AUTO-TOUCH-DROID - Configurações de Ambiente
# ============================================================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# Arquivo .env não deve ser commitado no git (já está no .gitignore)

# ============================================================================
# ADB (Android Debug Bridge)
# ============================================================================

# Caminho para o executável do ADB (deixe vazio para usar PATH do sistema)
ADB_PATH=adb

# ID do dispositivo padrão (deixe vazio para auto-detectar)
# Obtenha com: adb devices
# Exemplo: RXCTB03EXVK
# Configuração para Samsung Galaxy A73 5G
DEFAULT_DEVICE_ID=RXCTB03EXVK

# Timeout de conexão ADB em segundos
ADB_TIMEOUT=30

# ============================================================================
# Detecção de Imagem
# ============================================================================

# Threshold de confiança para template matching (0.0 a 1.0)
# Valores mais altos = mais preciso, mas pode não encontrar
# Valores mais baixos = menos preciso, mas encontra mais facilmente
DETECTION_THRESHOLD=0.8

# Número máximo de tentativas para encontrar um template
MAX_ATTEMPTS=5

# Delay entre tentativas (segundos)
ATTEMPT_DELAY=1.0

# Delay inicial antes de começar a buscar (segundos)
INITIAL_DELAY=2.0

# ============================================================================
# Performance
# ============================================================================

# Número máximo de workers para processamento paralelo
MAX_WORKERS=3

# Habilitar cache de screenshots (true/false)
SCREENSHOT_CACHE_ENABLED=true

# Habilitar cache de templates (true/false)
TEMPLATE_CACHE_ENABLED=true

# Duração do cache em segundos
CACHE_DURATION=1.0

# ============================================================================
# Logging
# ============================================================================

# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Tamanho máximo do arquivo de log em bytes (10MB padrão)
LOG_MAX_SIZE=10485760

# Número de arquivos de backup
LOG_BACKUP_COUNT=5

# ============================================================================
# Ambiente
# ============================================================================

# Ambiente: development, production, testing
ENVIRONMENT=development

# Modo debug (true/false)
DEBUG=false

# ============================================================================
# Caminhos Customizados (Opcional)
# ============================================================================

# Pasta de ações (deixe vazio para usar padrão: backend/actions/templates)
# ACTIONS_FOLDER=

# Pasta de screenshots temporários (deixe vazio para usar padrão: temp_screenshots)
# SCREENSHOTS_FOLDER=

# Pasta de logs (deixe vazio para usar padrão: logs)
# LOGS_FOLDER=

# ============================================================================
# API REST (Futuro)
# ============================================================================

# Porta do servidor API
# API_PORT=5000

# Host do servidor API
# API_HOST=0.0.0.0

# Habilitar CORS
# API_CORS_ENABLED=true

# ============================================================================
# Notas
# ============================================================================
# 
# 1. Valores booleanos devem ser: true ou false (minúsculas)
# 2. Caminhos podem ser absolutos ou relativos ao diretório do projeto
# 3. Variáveis comentadas (#) usarão valores padrão do código
# 4. Não use aspas nos valores (exceto se fizerem parte do valor)
#
# ============================================================================
